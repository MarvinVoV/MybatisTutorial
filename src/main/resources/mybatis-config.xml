<?xml version = "1.0" encoding = "UTF-8"?>

<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

    <properties resource="config.properties"/>

    <settings>
        <setting name="cacheEnabled" value="false"/>
    </settings>

    <typeAliases>
        <package name="com.marvin.mybatis.tutorial.entity"/>
    </typeAliases>

    <typeHandlers>
        <typeHandler handler="com.marvin.mybatis.tutorial.typehandlers.ExampleTypeHandler"/>
    </typeHandlers>

    <plugins>
        <plugin interceptor="com.marvin.mybatis.tutorial.plugins.ExamplePlugin">
            <property name="someThing" value="100"/>
        </plugin>
    </plugins>

    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>

            <dataSource type="POOLED">
                <property name="driver" value="${driver}"/>
                <property name="url" value="${url}"/>
                <property name="username" value="${username}"/>
                <property name="password" value="${password}"/>
            </dataSource>

        </environment>
    </environments>


    <mappers>
        <mapper resource="mapper/Student.xml"/>
    </mappers>

</configuration>
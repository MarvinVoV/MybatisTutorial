<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.marvin.mybatis.tutorial.mappers.Author">
    <resultMap type="Author" id="result">
        <result property="id" column="id" javaType="int"/>
        <result property="username" column="username" javaType="string"/>
        <result property="password" column="password" javaType="string"/>
        <result property="bio" column="bio" javaType="string"/>
        <result property="email" column="email" javaType="string"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" statementType="PREPARED">
        insert into Author (username, password, email, bio)
        values (#{username}, #{password}, #{email}, #{bio})
    </insert>

    <update id="update" statementType="PREPARED">
        update Author
        set username = #{username},
            password = #{password},
            email    = #{email},
            bio      = #{bio}
        where id = #{id}
    </update>

    <delete id="delete" statementType="PREPARED">
        delete
        from Author
        where id = #{id}
    </delete>
</mapper>